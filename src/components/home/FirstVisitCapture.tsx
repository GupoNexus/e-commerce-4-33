import { useEffect, useState } from "react";
import { MessageCircle, X } from "lucide-react";
import { siteConfig } from "@/config/siteConfig";
export function FirstVisitCapture() { const cfg = siteConfig.home.firstVisit; const [open, setOpen] = useState(false); const [consent, setConsent] = useState(false); useEffect(() => { if (!cfg.enabled || localStorage.getItem("site-first-visit-seen")) return; const timer = window.setTimeout(() => setOpen(true), cfg.delayMs); return () => window.clearTimeout(timer); }, [cfg.enabled, cfg.delayMs]); const close = () => { localStorage.setItem("site-first-visit-seen", "1"); setOpen(false); }; if (!open) return null; const href = `https://wa.me/${siteConfig.contact.whatsapp}?text=${encodeURIComponent(siteConfig.contact.whatsappMessage)}`; return <div className="fixed inset-0 z-[80] bg-black/35 flex items-end sm:items-center justify-center p-3" role="dialog" aria-modal="true"><div className="relative bg-white rounded-3xl p-6 sm:p-8 max-w-md w-full shadow-2xl"><button onClick={close} aria-label="Fechar" className="absolute right-4 top-4 p-2 text-muted-foreground"><X className="w-5 h-5" /></button><p className="text-[10px] font-bold uppercase tracking-[0.3em] text-primary/60">Bem-vindo</p><h2 className="text-2xl font-bold text-primary mt-2">{cfg.title}</h2><p className="text-sm text-muted-foreground mt-3">{cfg.description}</p><label className="flex items-start gap-3 mt-5 text-xs text-muted-foreground"><input type="checkbox" checked={consent} onChange={(e) => setConsent(e.target.checked)} className="mt-0.5" />Concordo em iniciar o atendimento pelo WhatsApp.</label><a href={consent ? href : undefined} target="_blank" rel="noreferrer" onClick={() => consent && close()} className={`mt-5 flex items-center justify-center gap-2 w-full rounded-full py-3 text-xs font-bold uppercase tracking-wider ${consent ? "bg-primary text-white" : "bg-muted text-muted-foreground pointer-events-none"}`}><MessageCircle className="w-4 h-4" />Quero atendimento</a></div></div>; }
