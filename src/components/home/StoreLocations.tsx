import { MapPin, Phone, MessageCircle, Navigation } from "lucide-react";
import { siteConfig } from "@/config/siteConfig";
export function StoreLocations() { const stores = siteConfig.home.stores; if (!stores.enabled || !stores.items.length) return null; return <section className="py-20 bg-secondary/10"><div className="container mx-auto px-4"><h2 className="text-2xl lg:text-4xl font-bold text-center mb-16 text-primary uppercase tracking-tighter">{stores.title}</h2><div className="grid grid-cols-1 md:grid-cols-2 gap-12 max-w-6xl mx-auto">{stores.items.map((store) => <div key={store.name} className="bg-white rounded-3xl overflow-hidden shadow-xl border border-border flex flex-col"><div className="p-8 space-y-6 flex-1"><h3 className="text-2xl font-bold text-primary uppercase tracking-tight">{store.name}</h3><div className="space-y-4 text-sm font-medium text-muted-foreground"><div className="flex gap-3"><MapPin className="w-5 h-5 text-primary shrink-0" /><p>{store.address}</p></div>{store.phone && <div className="flex gap-3"><Phone className="w-5 h-5 text-primary shrink-0" /><p>{store.phone}</p></div>}</div><div className="grid grid-cols-1 sm:grid-cols-2 gap-3 pt-6"><a href={store.mapsLink} target="_blank" rel="noopener noreferrer" className="flex items-center justify-center gap-2 bg-secondary text-primary py-3 rounded-full font-bold text-xs uppercase tracking-widest hover:bg-primary hover:text-white transition-all"><Navigation className="w-4 h-4" /> Como chegar</a><a href={`https://wa.me/${store.whatsapp}?text=${encodeURIComponent(siteConfig.contact.whatsappMessage)}`} target="_blank" rel="noopener noreferrer" className="flex items-center justify-center gap-2 bg-primary text-white py-3 rounded-full font-bold text-xs uppercase tracking-widest"><MessageCircle className="w-4 h-4" /> WhatsApp</a></div></div></div>)}</div></div></section>; }
