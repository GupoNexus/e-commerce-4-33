import { ShieldCheck, MessageCircle, Truck, Sparkles, Store, MapPin } from "lucide-react";
import { siteConfig } from "@/config/siteConfig";
const icons = { shield: ShieldCheck, message: MessageCircle, truck: Truck, sparkles: Sparkles, store: Store, map: MapPin } as const;
export function Benefits() { return <section className="bg-secondary/30 py-12"><div className="container mx-auto px-4"><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-8">{siteConfig.home.benefits.map((benefit, i) => { const Icon = icons[benefit.icon as keyof typeof icons] ?? Sparkles; return <div key={`${benefit.title}-${i}`} className="flex flex-col items-center text-center space-y-3 group"><div className="w-12 h-12 flex items-center justify-center rounded-full bg-white text-primary shadow-sm group-hover:bg-primary group-hover:text-white transition-all duration-300"><Icon className="w-6 h-6" /></div><div className="space-y-1"><h3 className="text-[10px] sm:text-xs font-bold uppercase tracking-widest text-primary">{benefit.title}</h3><p className="text-[10px] sm:text-[11px] text-muted-foreground font-medium">{benefit.description}</p></div></div>; })}</div></div></section>; }
