import { Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";
import { GUIDE_ARTICLES } from "@/data/guide";
import { siteConfig } from "@/config/siteConfig";
export function GuideSection() { if (!siteConfig.home.guide.enabled) return null; return <section className="py-16 bg-white"><div className="container mx-auto px-4"><div className="flex flex-col sm:flex-row sm:items-end justify-between gap-4 mb-10"><div><p className="text-[11px] font-bold uppercase tracking-[0.3em] text-primary/60">{siteConfig.home.guide.eyebrow}</p><h2 className="text-2xl lg:text-3xl font-bold text-primary mt-2">{siteConfig.home.guide.title}</h2><p className="text-sm text-muted-foreground mt-2">{siteConfig.home.guide.description}</p></div><Link to="/guia" className="text-xs font-bold uppercase tracking-wider text-primary flex items-center gap-2">Ver todos <ArrowRight className="w-4 h-4" /></Link></div><div className="grid md:grid-cols-3 gap-5">{GUIDE_ARTICLES.map((article, index) => <Link key={article.slug} to="/guia/$slug" params={{ slug: article.slug }} className="group rounded-2xl border border-border p-6 hover:shadow-lg transition-all"><span className="text-4xl font-bold text-primary/10">0{index + 1}</span><h3 className="font-bold text-primary text-lg mt-4">{article.title}</h3><p className="text-sm text-muted-foreground mt-2 leading-relaxed">{article.description}</p><span className="inline-flex items-center gap-2 mt-5 text-[11px] font-bold uppercase tracking-wider text-primary">Ler guia <ArrowRight className="w-3 h-3" /></span></Link>)}</div></div></section>; }
