import { siteConfig } from "@/config/siteConfig";
export function AnnouncementRibbon() { if (!siteConfig.announcement.enabled) return null; const phrase = siteConfig.announcement.items.join("  •  ") + "  •  "; const track = phrase.repeat(6); return <div className="relative w-full overflow-hidden py-2 shadow-sm border-b border-border" style={{ background: "linear-gradient(90deg, #eeeeee 0%, #fafafa 50%, #eeeeee 100%)" }}><div className="flex w-max animate-gold-marquee"><span className="whitespace-nowrap px-4 text-[11px] sm:text-xs font-bold uppercase tracking-[0.25em] text-primary/80">{track}</span><span className="whitespace-nowrap px-4 text-[11px] sm:text-xs font-bold uppercase tracking-[0.25em] text-primary/80">{track}</span></div></div>; }
