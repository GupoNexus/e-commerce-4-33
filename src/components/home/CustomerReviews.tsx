import { Star } from "lucide-react";
import { siteConfig } from "@/config/siteConfig";
export function CustomerReviews() { const r = siteConfig.home.reviews; if (!r.enabled) return null; return <section className="py-20 bg-white"><div className="container mx-auto px-4"><div className="text-center mb-16 space-y-4"><h2 className="text-2xl lg:text-3xl font-bold text-primary">O que dizem nossos clientes</h2><div className="flex items-center justify-center gap-2"><span className="text-2xl font-bold text-primary">{r.rating.toFixed(1)}</span><div className="flex text-yellow-400">{[...Array(5)].map((_, i) => <Star key={i} className="w-5 h-5 fill-current" />)}</div>{r.count > 0 && <span className="text-muted-foreground font-medium">({r.count.toLocaleString()} {r.sourceLabel})</span>}</div></div><div className="max-w-3xl mx-auto rounded-3xl border border-border bg-secondary/10 p-8 text-center"><p className="text-primary font-semibold text-lg">Use esta área para destacar a prova social da sua marca.</p><p className="text-sm text-muted-foreground mt-2">Configure a quantidade, nota e link das avaliações em <code>src/config/siteConfig.ts</code>.</p></div>{r.link !== "#" && <div className="text-center mt-12"><a href={r.link} target="_blank" rel="noreferrer" className="inline-block bg-primary text-white px-10 py-3 rounded-full font-bold uppercase tracking-widest hover:bg-primary/90 transition-all shadow-lg">Ver avaliações</a></div>}</div></section>; }
