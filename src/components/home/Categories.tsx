import { Link } from "@tanstack/react-router";
import { siteConfig } from "@/config/siteConfig";
export function Categories() { const categories = siteConfig.home.categories; return <section className="py-16 container mx-auto px-4"><div className="text-center mb-12"><h2 className="text-2xl lg:text-3xl font-bold text-primary">Compre por categoria</h2><p className="text-sm text-muted-foreground mt-2">Organize seus produtos em categorias e facilite a navegação.</p></div><div className="grid grid-cols-2 md:grid-cols-5 gap-4">{categories.map((cat) => <Link key={cat.name} to="/catalogo" search={{ categoria: cat.categoria as string }} className="group relative overflow-hidden rounded-2xl aspect-[4/5] cursor-pointer block"><img src={cat.image} alt={`Categoria ${cat.name}`} loading="lazy" className="absolute inset-0 w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" /><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/10 to-transparent flex items-end p-4"><div><h3 className="text-white font-bold text-lg">{cat.name}</h3><p className="text-white/80 text-[11px] mt-1 hidden sm:block">{cat.text}</p><span className="inline-block text-white text-[10px] font-bold uppercase tracking-widest mt-3 border-b border-white/70">Explorar</span></div></div></Link>)}</div></section>; }
