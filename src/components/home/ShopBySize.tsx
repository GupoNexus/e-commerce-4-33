import { Link } from "@tanstack/react-router";
import { Ruler } from "lucide-react";
import { siteConfig } from "@/config/siteConfig";
export function ShopBySize() { const cfg = siteConfig.home.shopBySize; if (!cfg.enabled || !cfg.sizes.length) return null; return <section className="py-16 bg-secondary/25"><div className="container mx-auto px-4"><div className="text-center mb-10"><h2 className="text-2xl lg:text-3xl font-bold text-primary">{cfg.title}</h2></div><div className="grid grid-cols-2 lg:grid-cols-4 gap-4 max-w-5xl mx-auto">{cfg.sizes.map((size) => <Link key={size} to="/catalogo" search={{ tamanho: size }} className="group bg-white border border-border rounded-2xl p-6 text-center hover:-translate-y-1 hover:shadow-lg transition-all"><Ruler className="w-7 h-7 text-primary mx-auto mb-3 group-hover:scale-110 transition-transform" /><h3 className="font-bold text-primary uppercase tracking-wider">{size}</h3><span className="text-[10px] text-muted-foreground uppercase tracking-widest">Explorar</span></Link>)}</div></div></section>; }
