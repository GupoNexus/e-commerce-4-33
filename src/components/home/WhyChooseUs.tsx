import { CheckCircle } from "lucide-react";
import { siteConfig } from "@/config/siteConfig";
export function WhyChooseUs() { const why = siteConfig.home.why; if (!why.enabled) return null; return <section className="py-20 bg-secondary/20"><div className="container mx-auto px-4 text-center max-w-4xl"><h2 className="text-3xl lg:text-4xl font-bold text-primary mb-6 italic">"{why.title}"</h2><p className="text-lg text-muted-foreground mb-16 font-medium">{why.description}</p><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">{why.highlights.map((item) => <div key={item.title} className="space-y-4"><div className="flex justify-center"><CheckCircle className="w-8 h-8 text-primary" /></div><h3 className="text-sm font-bold uppercase tracking-widest text-primary">{item.title}</h3><p className="text-xs text-muted-foreground leading-relaxed">{item.text}</p></div>)}</div></div></section>; }
